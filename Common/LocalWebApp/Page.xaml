<Page
    x:Class="Trivial.UI.LocalWebAppPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Trivial.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid ActualThemeChanged="OnActualThemeChanged" >
        <Grid.RowDefinitions>
            <RowDefinition Height="0" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <WebView2 x:Name="Browser" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CoreWebView2Initialized="OnCoreWebView2Initialized" NavigationCompleted="OnNavigationCompleted" NavigationStarting="OnNavigationStarting" WebMessageReceived="OnWebMessageReceived" CoreProcessFailed="OnCoreProcessFailed" Grid.Row="1" />
        <ProgressRing x:Name="ProgressElement" IsActive="True" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="100" Grid.Row="1" />
        <InfoBar x:Name="NotificationBar" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20" Grid.Row="1" />
    </Grid>
</Page>
